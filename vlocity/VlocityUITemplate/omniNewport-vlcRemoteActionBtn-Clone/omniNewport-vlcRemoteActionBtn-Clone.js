vlocity.cardframework.registerModule.controller('buttonDocuController', ['$scope', '$timeout', function($scope, $timeout) {
   /*$scope.hasPDF = false;
   $scope.$watch("bpTree.response.GenerateDocument.FormulaHasPdf", function(newValue,oldValue){
       $scope.hasPDF = newValue;
   });

   $scope.loading = true;

   stop = $timeout(function() {
        $scope.loading = false;
    }, 45000);
    */
}]);